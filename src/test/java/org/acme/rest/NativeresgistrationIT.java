package org.acme.rest;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeresgistrationIT extends resgistrationTest {

    // Execute the same tests but in native mode.
}